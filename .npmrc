# Force clean installations
force=true
# Ensure packages are installed even with warnings
loglevel=error
# Don't generate a package-lock.json to avoid conflicts
package-lock=false
# Allow legacy peer dependencies for compatibility
legacy-peer-deps=true
fund=false
audit=false 