import { PageWrapper } from '@/components/PageWrapper'

export default function CyberNexPlusLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <PageWrapper>{children}</PageWrapper>
} 